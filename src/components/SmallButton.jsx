import styles from '../styles/components/SmallButton.module.scss'

const SmallButton = () => {
  return (
    <button type='button' className={styles['small-button']}>
    </button>
  )
}

export default SmallButton
